pipeline {
    agent none
    options {
        disableConcurrentBuilds()
    }
    environment {
        AWS_DEFAULT_REGION = 'us-east-1'
    }
    stages {
        stage('Test, Static Analysis and Build') {
            echo "You did it!"    
	}
    }
}
